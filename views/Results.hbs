<html>

<head>
    <!--Load the AJAX API-->
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">

        // Load the Visualization API and the piechart package.
        google.charts.load('current', { 'packages': ['corechart'] });

        // Set a callback to run when the Google Visualization API is loaded.
        google.charts.setOnLoadCallback(drawChart);

        //const app.get
        /*
        //may set i to less than 40
            var newUid,i,;
            var uid = [];
                for ( i = 0; i < uid.length; i++) {
                    if (uid[i] === newUid) { // the third equal sin checks the data type
                        break;
                    }
                    else{
                        uid.push(newUid);
                    }
                }
                return//
        */
        console.log("summonning the json data forth from the depth is of the server")
        async function getData() {
            const response = await fetch();
            const jsonData = await response.json();
            console.log(jsonData)

            var uid
            function drawChart() {
                // var jsonData = $.ajax({
                // url: "test.json",jkk
                // dataType: "json",
                // async: true
                // }).responseText;

                // Create our data table out of JSON data loaded from server.
                var data = new google.visualization.DataTable(jsonData);

                // Instantiate and draw our chart, passing in some options.
                var chart = new google.visualization.PieChart(document.getElementById('chart_div'));
                chart.draw(data, { width: 400, height: 240 });
            }
        }
        //settting refresh rate to  about 1 second
        setInterval(getData(), 1000);

    </script>
</head>

<body>
    <!--Div that will hold the pie chart-->
    <div id="chart_div"></div>
</body>

</html><html>

<head>
    <!--Load the AJAX API-->
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">

        // Load the Visualization API and the piechart package.
        google.charts.load('current', { 'packages': ['corechart'] });

        // Set a callback to run when the Google Visualization API is loaded.
        google.charts.setOnLoadCallback(drawChart);

        //const app.get

        //may set i to less than 40

        const api_url = server + '/poll'
        console.log("summonning the json data forth from the depth is of the server")
        async function getData() {
            const response = await fetch(api_url, {
                method:"GET"
            });

            let data = 


            var uid = [];
            for (var i = 0; i < uid.length; i++) {
                if (Jid === uid[i]) { // the third equal sin checks the data type
                    break;
                }
                else {
                    uid.push(Jid);
                }
            }
            return

            function drawChart() {
                // var jsonData = $.ajax({
                // url: "test.json",jkk
                // dataType: "json",
                // async: true
                // }).responseText;

                // Create our data table out of JSON data loaded from server.
                var data = new google.visualization.DataTable(jsonData);

                // Instantiate and draw our chart, passing in some options.
                var chart = new google.visualization.PieChart(document.getElementById('chart_div'));
                chart.draw(data, { width: 400, height: 240 });
            }
        }
        //settting refresh rate to  about 1 second
        setInterval(getData(), 1000);

    </script>
</head>

<body>
    <!--Div that will hold the pie chart-->
    <div id="chart_div"></div>
</body>

</html>